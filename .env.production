# Production configuration for paybuddy.site hosting
# Serve at domain root
VITE_BASE="/"

# Backend API base
VITE_API_BASE="https://api.paybuddy.site"

# If your backend only serves HTTPS and you need a proxy override for mixed http->https, set below
# VITE_API_HTTPS_PROXY="https://api.paybuddy.site"
