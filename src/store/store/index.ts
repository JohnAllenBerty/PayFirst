// Deprecated compatibility shim for legacy dynamic import path '../store/store'.
// Prefer importing from '../store.ts' directly: `import { store } from "./store"`.
// This shim will be removed in a future cleanup.
export { store } from '../store'
